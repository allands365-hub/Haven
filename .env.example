# Copy to .env.local and fill in. .env.local is git-ignored.
#
# The OpenAI API key is NOT stored here — it's set server-side in Convex:
#   bunx convex env set OPENAI_API_KEY <your-key>
#
# The frontend only needs the Convex deployment URL (filled by `bunx convex dev`):
NEXT_PUBLIC_CONVEX_URL=
